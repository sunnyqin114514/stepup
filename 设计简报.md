# StepUp 设计简报 — 供 AI UI 设计工具评估

## 项目概述

**StepUp** 是一款 AI 驱动的学习搭子网站，帮用户把大目标拆解成每天做得到的小事。

- **Slogan**：把大目标，变成今天做得到的小事
- **目标用户**：学生（备考、学习计划）和职场新人（个人成长目标）
- **技术栈**：Vite + React 19 + TypeScript + Tailwind CSS 3.4 + Netlify Functions
- **灵感参考**：wanderaiai.netlify.app

## 四个核心功能

| 功能 | 说明 | 页面 |
|------|------|------|
| AI 任务拆解 | 输入大目标+截止日期，AI 生成每日任务 | PlannerPage |
| 专注打卡 | 任务卡内嵌计时器（正计时/倒计时），完成打卡 | SchedulePage + TaskCard |
| 知识库（Pro） | 沉淀笔记/错题/心得，关联任务 | KnowledgePage（mock） |
| 复习提醒（Pro） | 艾宾浩斯遗忘曲线提醒复习 | ReviewPage（mock） |

## 页面结构（8 页）

1. **HomePage** — 首页：Hero + 4 功能卡片 + 四步闭环 + CTA
2. **PlannerPage** — 目标规划：AI 拆解 + 多目标管理 + 用量进度条
3. **SchedulePage** — 学习日：今日任务列表 + 专注计时 + 打卡/跳过 + 未完成池
4. **CheckInPage** — 打卡完成庆祝页
5. **ProgressPage** — 复盘：统计 + 成就 + 未完成池 + 明日预告
6. **KnowledgePage** — 知识库（Pro mock 展示版）
7. **ReviewPage** — 复习提醒（Pro mock 展示版）
8. **MembershipPage** — 会员付费墙

## 当前设计系统

### 品牌色（tailwind.config.js）

```
brand（主色，蓝色系）:
  50: #f0f7ff  →  500: #0c8ee6  →  900: #0b416e

accent（强调色，琥珀色）:
  500: #f59e0b  →  600: #d97706
```

### 组件类（src/index.css）

- `.btn-primary` — 蓝底白字圆角按钮 + 阴影
- `.btn-ghost` — 白底灰边框按钮
- `.card` — 白底圆角卡片 + 微阴影
- `.input-field` — 圆角输入框 + focus 蓝色边框
- `.pro-badge` — 琥珀渐变 Pro 标签

### 字体

```
-apple-system, BlinkMacSystemFont, PingFang SC, Microsoft YaHei, sans-serif
```

### 动画

- `fade-in` / `slide-up` / `pulse-ring`

## 希望评估和设计的方向

### 1. 整体 UI 风格统一性

- 按钮风格是否一致？（btn-primary / btn-ghost 使用是否合理）
- 卡片层次是否清晰？（card 统一用法，是否有视觉层级问题）
- 输入框、弹窗、标签的圆角/边框/间距是否成体系？
- 色彩使用是否克制？brand 蓝和 accent 琥珀的搭配是否协调？

### 2. 按键与交互组件设计

- 按钮 hover/active/disabled 状态是否到位？
- Modal 弹窗的遮罩、动画、关闭交互是否需要优化？
- TaskCard 任务卡的信息密度和视觉重点是否合理？
- 计时器在任务卡中的位置和展示方式是否最优？

### 3. 背景与视觉氛围

- 目前全站浅色底（#f8fafc），无暗色模式
- 首页 Hero 用了 `bg-gradient-to-b from-brand-50 via-white to-white`，比较平
- 希望评估：是否需要加入动态/高级感背景？
- 已探索过 4 款背景方向（极光渐变/星轨矩阵/晨曦柔光/阶梯流光），可参考

### 4. 响应式与移动端

- 导航在移动端是横向滚动条，是否需要改进？
- 任务卡在窄屏下的信息布局是否合理？
- 底部固定操作栏在移动端的体验如何？

### 5. 情感化设计

- 学习产品需要激励感，目前的视觉反馈是否足够？
- 任务完成、打卡成功、连续天数等时刻的情感化设计是否到位？
- 空状态的插画/引导是否有温度？

## 文件结构说明

```
src/
├── pages/          8 个页面组件
├── components/     Layout / TaskCard / ProBadge
├── hooks/          useTaskTimer（计时器逻辑）
├── services/       planApi（AI 接口封装）
├── lib/            storage（localStorage 数据层）
├── types/          plan（TypeScript 类型定义）
├── App.tsx         路由
├── main.tsx        入口
└── index.css       全局样式 + 组件类

tailwind.config.js  品牌色 / 字体 / 动画
netlify/functions/  AI 后端函数
```

## 已知问题（详见评审报告）

- 无暗色模式
- Modal 缺少 ESC/focus trap
- TaskCard 完成无动效
- 首页主 CTA 指向空页面
- 对比度部分不达 WCAG AA
- 无页面切换动画

## 期望输出

1. **整体 UI 风格评估** — 当前设计的优点和问题
2. **按键/组件风格建议** — 按钮、卡片、输入框、弹窗的优化方向
3. **背景方案建议** — 适合学习产品的背景风格
4. **配色优化** — brand 蓝色系是否需要调整
5. **移动端体验** — 响应式改进建议
